# Both variables are optional.
# If SECRET is empty or invalid, the container creates a stable per-service value automatically.
# To override it manually, generate one with: openssl rand -hex 16
SECRET=
WORKERS=2
